{
  "license": "MIT",
  "dependencies": {
    "axios": "^0.16.2",
    "fb": "^2.0.0",
    "@syncano/core": "^0.7.1"
  },
  "scripts": {
    "build": "npm && npm run build:src && npm run build:env",
    "build:src": "sh ./bin/compile",
    "build:env": "sh ./bin/compile-env"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "8"
          }
        }
      ]
    ]
  }
}
